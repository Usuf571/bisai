# BiSAI - Team Quiz Game (Перетягивание Каната)

## 📋 Описание проекта

BiSAI - это веб-приложение для проведения интерактивных викторин между двумя командами. Механика игры похожа на "Перетягивание каната" - при каждом правильном ответе команда "тянет канат" в свою сторону.

### Основные возможности:
- ✅ Система авторизации и регистрации пользователей
- ✅ Создание собственных тестов с вопросами и ответами
- ✅ Управление созданными тестами (редактирование, публикация, удаление)
- ✅ Игра в реальном времени между двумя командами
- ✅ Анимированное "перетягивание каната"
- ✅ Фоновая музыка во время игры
- ✅ Статистика по игрокам и тестам
- ✅ Личный кабинет с управлением тестами

---

## 📁 Архитектура проекта

```
bisai/
├── config/                          # Конфигурационные файлы
│   ├── app.php                      # Параметры приложения
│   ├── db.php                       # Параметры подключения к БД
│   └── constants.php                # Глобальные константы
│
├── database/                        # Работа с БД
│   ├── migrations/                  # SQL миграции
│   │   └── 001_create_initial_tables.sql
│   └── seeders/                     # Начальные данные
│
├── includes/                        # Вспомогательные файлы
│   ├── Database.php                 # Класс управления БД
│   ├── autoload.php                 # Автозагрузка классов
│   ├── functions.php                # Вспомогательные функции
│   └── auth.php                     # Функции авторизации
│
├── models/                          # Модели данных (ORM)
│   ├── BaseModel.php                # Базовый класс модели
│   ├── User.php                     # Модель пользователя
│   ├── Test.php                     # Модель теста
│   ├── Question.php                 # Модель вопроса
│   └── GameSession.php              # Модель игровой сессии
│
├── controllers/                     # Контроллеры (бизнес-логика)
│   ├── BaseController.php           # Базовый класс контроллера
│   ├── AuthController.php           # Авторизация и регистрация
│   ├── TestController.php           # Управление тестами
│   └── GameController.php           # Игровая логика
│
├── public/                          # Доступно через веб (точка входа)
│   ├── index.php                    # Главная страница
│   ├── router.php                   # Маршрутизатор
│   ├── api.php                      # API точка для AJAX запросов
│   ├── login.php                    # Вход
│   ├── register.php                 # Регистрация
│   ├── logout.php                   # Выход
│   ├── dashboard.php                # Личный кабинет
│   ├── create_test.php              # Создание теста
│   ├── edit_test.php                # Редактирование теста
│   ├── delete_test.php              # Удаление теста
│   ├── play.php                     # Игра
│   │
│   ├── assets/
│   │   ├── css/
│   │   │   ├── main.css             # Основные стили
│   │   │   ├── auth.css             # Стили авторизации
│   │   │   └── game.css             # Стили игры (канат, команды)
│   │   │
│   │   ├── js/
│   │   │   ├── main.js              # Основной скрипт
│   │   │   ├── auth.js              # Скрипты авторизации
│   │   │   ├── game.js              # Логика игры
│   │   │   └── tug-of-war.js        # Анимация каната
│   │   │
│   │   ├── audio/
│   │   │   └── background.mp3       # Фоновая музыка
│   │   │
│   │   └── images/
│   │       ├── icons/
│   │       └── game/
│   │           ├── rope.png
│   │           ├── flag-red.png
│   │           └── flag-blue.png
│   │
│   └── views/                       # Шаблоны (HTML)
│       ├── home.php                 # Главная страница
│       ├── dashboard.php            # Личный кабинет
│       │
│       ├── layout/
│       │   ├── header.php           # Навигация и заголовок
│       │   ├── footer.php           # Подвал
│       │   └── nav.php              # Меню навигации
│       │
│       ├── auth/
│       │   ├── login.php            # Форма входа
│       │   └── register.php         # Форма регистрации
│       │
│       ├── tests/
│       │   ├── list.php             # Список всех тестов
│       │   ├── select.php           # Выбор теста перед игрой
│       │   ├── create.php           # Форма создания теста
│       │   ├── edit.php             # Форма редактирования теста
│       │   └── questions/
│       │       ├── form.php         # Форма добавления вопроса
│       │       └── list.php         # Список вопросов
│       │
│       └── game/
│           ├── setup.php            # Ввод имен команд
│           ├── play.php             # Игровой интерфейс
│           └── results.php          # Результаты игры
│
└── README.md                        # Этот файл

```

---

## 🗄️ Структура базы данных

### Таблица: `bisai_users` (Пользователи)
```sql
- id (INT PRIMARY KEY AUTO_INCREMENT)
- username (VARCHAR 50, UNIQUE)
- email (VARCHAR 100, UNIQUE)
- password_hash (VARCHAR 255)
- first_name (VARCHAR 100)
- last_name (VARCHAR 100)
- role (ENUM: 'admin', 'user', 'guest')
- avatar_url (VARCHAR 255)
- is_active (BOOLEAN)
- created_at (TIMESTAMP)
- updated_at (TIMESTAMP)
- last_login (TIMESTAMP)
```

### Таблица: `bisai_tests` (Тесты)
```sql
- id (INT PRIMARY KEY AUTO_INCREMENT)
- creator_id (INT FOREIGN KEY)
- title (VARCHAR 200)
- description (LONGTEXT)
- difficulty (ENUM: 'easy', 'medium', 'hard')
- status (ENUM: 'draft', 'published', 'archived')
- is_public (BOOLEAN)
- min_players_per_team (INT)
- max_players_per_team (INT)
- category (VARCHAR 100)
- duration_estimate (INT)
- created_at (TIMESTAMP)
- updated_at (TIMESTAMP)
- deleted_at (TIMESTAMP)
```

### Таблица: `bisai_questions` (Вопросы)
```sql
- id (INT PRIMARY KEY AUTO_INCREMENT)
- test_id (INT FOREIGN KEY)
- question_text (LONGTEXT)
- question_type (ENUM: 'multiple_choice', 'true_false', 'short_answer')
- image_url (VARCHAR 255)
- audio_url (VARCHAR 255)
- explanation (LONGTEXT)
- order (INT)
- is_mandatory (BOOLEAN)
- created_at (TIMESTAMP)
```

### Таблица: `bisai_answers` (Ответы на вопросы)
```sql
- id (INT PRIMARY KEY AUTO_INCREMENT)
- question_id (INT FOREIGN KEY)
- answer_text (LONGTEXT)
- is_correct (BOOLEAN)
- order (INT)
- created_at (TIMESTAMP)
```

### Таблица: `bisai_game_sessions` (Игровые сессии)
```sql
- id (INT PRIMARY KEY AUTO_INCREMENT)
- test_id (INT FOREIGN KEY)
- session_code (VARCHAR 20, UNIQUE)
- status (ENUM: 'waiting', 'in_progress', 'finished')
- team_red_name (VARCHAR 100)
- team_blue_name (VARCHAR 100)
- current_question_index (INT)
- started_at (TIMESTAMP)
- finished_at (TIMESTAMP)
- created_at (TIMESTAMP)
```

### Таблица: `bisai_game_players` (Игроки в игре)
```sql
- id (INT PRIMARY KEY AUTO_INCREMENT)
- game_session_id (INT FOREIGN KEY)
- user_id (INT FOREIGN KEY, nullable)
- player_name (VARCHAR 100)
- team_color (ENUM: 'red', 'blue')
- score (INT)
- correct_answers (INT)
- total_answers (INT)
- joined_at (TIMESTAMP)
```

### Таблица: `bisai_player_answers` (Ответы игроков)
```sql
- id (INT PRIMARY KEY AUTO_INCREMENT)
- game_session_id (INT FOREIGN KEY)
- game_player_id (INT FOREIGN KEY)
- question_id (INT FOREIGN KEY)
- answer_id (INT FOREIGN KEY)
- answer_text (LONGTEXT)
- is_correct (BOOLEAN)
- response_time (INT)
- answered_at (TIMESTAMP)
```

### Таблица: `bisai_user_statistics` (Статистика пользователя)
```sql
- id (INT PRIMARY KEY AUTO_INCREMENT)
- user_id (INT FOREIGN KEY UNIQUE)
- total_games_played (INT)
- total_games_won (INT)
- total_games_lost (INT)
- total_score (INT)
- total_correct_answers (INT)
- total_answers (INT)
- average_accuracy (DECIMAL 5,2)
- tests_created (INT)
- updated_at (TIMESTAMP)
```

---

## 🚀 Установка и настройка

### 1. Подготовка БД

1. Откройте **phpMyAdmin**
2. Создайте новую БД с именем `bisai_db`
3. Выберите эту БД
4. Перейдите в раздел **SQL**
5. Скопируйте содержимое файла `database/migrations/001_create_initial_tables.sql`
6. Выполните SQL скрипт

### 2. Конфигурация подключения к БД

Отредактируйте файл `config/db.php`:

```php
return [
    'driver' => 'mysql',
    'host' => 'localhost',        // Хост БД (обычно localhost)
    'port' => 3306,              // Порт БД (обычно 3306)
    'database' => 'bisai_db',    // Имя БД
    'username' => 'root',        // Пользователь БД
    'password' => '',            // Пароль БД
    // ...
];
```

### 3. Настройка веб-сервера

#### Для Apache с mod_rewrite:
Создайте файл `.htaccess` в папке `public/`:
```apache
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]
</IfModule>
```

#### Для других серверов:
Перейдите на `index.php` явно в адресной строке.

---

## 📝 Использование API

### Контроллеры и методы

#### AuthController
- `login()` - Показать форму входа
- `handleLogin()` - Обработать вход
- `register()` - Показать форму регистрации
- `handleRegister()` - Обработать регистрацию
- `logout()` - Выход из системы

#### TestController
- `listTests()` - Показать все тесты
- `createForm()` - Форма создания теста
- `store()` - Сохранить новый тест
- `editForm($testId)` - Форма редактирования теста
- `update($testId)` - Обновить тест
- `addQuestion($testId)` - Добавить вопрос
- `delete($testId)` - Удалить тест
- `publish($testId)` - Опубликовать тест

#### GameController
- `setup($testId)` - Форма выбора команд
- `createSession()` - Создать игровую сессию
- `play($sessionCode)` - Показать игру
- `addPlayer()` - Добавить игрока
- `startGame()` - Начать игру
- `getCurrentQuestion()` - Получить текущий вопрос
- `submitAnswer()` - Отправить ответ
- `nextQuestion()` - Перейти к следующему вопросу
- `getGameState()` - Получить состояние игры
- `getResults()` - Получить результаты

---

## 🎮 Механика игры

### Процесс игры:

1. **Выбор теста** - Пользователь выбирает опубликованный тест
2. **Ввод имен команд** - Вводятся имена для красной и синей команды
3. **Создание сессии** - Создается игровая сессия с уникальным кодом
4. **Добавление игроков** - Игроки присоединяются к команде
5. **Начало игры** - Игра стартует, показывается первый вопрос
6. **Ответы** - Игроки отвечают на вопрос, неправильные ответы не дают очков
7. **Анимация каната** - При каждом ответе команда "тянет" канат
8. **Следующий вопрос** - После всех ответов переход к следующему вопросу
9. **Завершение** - После всех вопросов показываются результаты

### Расчет баллов:

- Правильный ответ = **+1 очко** для команды
- Неправильный ответ = **0 очков**
- Команда с большим количеством очков **побеждает**

---

## 🛠️ Технический стек

### Backend:
- **PHP 7.4+** - Язык программирования
- **MySQL 5.7+** - База данных
- **PDO** - Работа с БД
- **OOP** - Объектно-ориентированное программирование

### Frontend:
- **HTML5** - Разметка
- **CSS3** - Стилизация
- **JavaScript (Vanilla)** - Интерактивность
- **Canvas** - Анимация каната
- **Web Audio API** - Фоновая музыка

### Паттерны проектирования:
- **MVC** (Model-View-Controller)
- **Singleton** (Database)
- **Repository** (Models)
- **ORM** (Object-Relational Mapping)

---

## 🔒 Безопасность

### Реализованные меры:
- ✅ **Хеширование паролей** - Bcrypt algorithm
- ✅ **Защита от SQL-инъекций** - Подготовленные запросы (PDO)
- ✅ **Сессионная авторизация** - $_SESSION
- ✅ **CSRF защита** - Может быть добавлена
- ✅ **Валидация входных данных** - На уровне контроллера
- ✅ **Проверка прав доступа** - Только создатель может редактировать тест

---

## 📊 Примеры использования

### Регистрация пользователя:
```php
$user = User::register([
    'username' => 'john_doe',
    'email' => 'john@example.com',
    'password' => 'secure_password',
    'first_name' => 'John',
    'last_name' => 'Doe',
]);
```

### Создание теста:
```php
$test = new Test();
$test->attributes = [
    'creator_id' => $userId,
    'title' => 'Математика 101',
    'description' => 'Базовые математические операции',
    'difficulty' => 'easy',
    'status' => 'draft',
];
$test->save();
```

### Добавление вопроса:
```php
$question = new Question();
$question->attributes = [
    'test_id' => $testId,
    'question_text' => 'Сколько будет 2 + 2?',
    'question_type' => 'multiple_choice',
    'order' => 1,
];
$question->save();

// Добавить варианты ответов
$question->addAnswer(['answer_text' => '3', 'is_correct' => false, 'order' => 1]);
$question->addAnswer(['answer_text' => '4', 'is_correct' => true, 'order' => 2]);
$question->addAnswer(['answer_text' => '5', 'is_correct' => false, 'order' => 3]);
```

### Создание игровой сессии:
```php
$session = GameSession::createNew(
    $testId,
    'Красная команда',
    'Синяя команда'
);

// Добавить игроков
$session->addPlayer('Алиса', 'red', $userId1);
$session->addPlayer('Боб', 'red', $userId2);
$session->addPlayer('Вера', 'blue', $userId3);
$session->addPlayer('Геннадий', 'blue', $userId4);

// Начать игру
$session->start();
```

---

## 🎨 Интерфейс "Перетягивание каната"

### HTML структура:
```html
<div class="tug-of-war">
    <div class="team red">
        <div class="flag">🚩</div>
        <h3>Красная команда</h3>
        <p class="score">Score: <span id="red-score">0</span></p>
    </div>
    
    <div class="rope-container">
        <div class="rope">
            <div class="marker"></div>
        </div>
        <canvas id="tug-of-war-canvas"></canvas>
    </div>
    
    <div class="team blue">
        <div class="flag">🚩</div>
        <h3>Синяя команда</h3>
        <p class="score">Score: <span id="blue-score">0</span></p>
    </div>
</div>
```

### JavaScript логика:
```javascript
// Получить состояние игры
fetch('api.php?action=getGameState', {
    method: 'POST',
    body: JSON.stringify({ session_code: sessionCode })
})
.then(response => response.json())
.then(data => {
    // Обновить позицию каната на основе разницы в очках
    const redScore = data.team_red.score;
    const blueScore = data.team_blue.score;
    const position = (redScore - blueScore) * 10; // пиксели
    
    // Анимировать канат
    animateRope(position);
});
```

---

## 📱 Фоновая музыка

### Интеграция аудио:
```html
<audio id="background-music" loop>
    <source src="/assets/audio/background.mp3" type="audio/mpeg">
</audio>

<button onclick="toggleMusic()">🔊 Включить музыку</button>
```

```javascript
function toggleMusic() {
    const audio = document.getElementById('background-music');
    if (audio.paused) {
        audio.play();
    } else {
        audio.pause();
    }
}
```

---

## 🐛 Отладка и логирование

Все ошибки логируются в файл `logs/app.log`:
```php
$logger = new Logger();
$logger->error('Error message', ['context' => $data]);
```

---

## 📞 Поддержка

Для вопросов и багов откройте issue в GitHub репозитории.

---

## 📄 Лицензия

MIT License - Свободно используйте в личных и коммерческих проектах.

---

**Версия:** 1.0.0  
**Последнее обновление:** 2025-12-10  
**Автор:** Usuf571

